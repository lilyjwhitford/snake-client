const IP = "localhost";
const PORT = 50541;
const PLAYER_NAME = "LW";

const MOVE_KEYS = {
  'w': 'up',
  's': 'down',
  'a': 'left',
  'd': 'right'
};

const MSG_KEYS = {
    "j": "woohoo!",
    "k": "womp womp"
};

module.exports = {
  IP,
  PORT,
  PLAYER_NAME,
  MOVE_KEYS,
  MSG_KEYS
};